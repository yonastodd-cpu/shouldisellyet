# Final go-live review

**Run 2026-08-27, 01:30–03:00 UTC · reviewed against production (shouldisellyet.com + Supabase `kfbjooteazwvdsonthba`) · repo at `18b4216` → fixes landed as `92dfb95`.**

Legend: 🟢 pass · 🟡 ships with the dated note in its row · 🔴 launch blocker.
Two findings were **fixed during the review** and re-verified; their rows say so.
Read-only against production except the probes each row names.

## A. Compliance state

| # | Check | Status | Evidence |
|---|---|---|---|
| 1 | Gate suite A–E, fresh | 🟢 | Fresh runs 2026-08-27: **A** `crawl-gate.mjs https://shouldisellyet.com --sample 60` → 46,427 URLs discovered, 154 crawled, `clean`. **B** `gate-paid-surfaces.py` → PASS, 2 paid surfaces, 17,874 notice pages figure-free. **C** `gate-db-tripwire.py` → PASS, 4 accesses each guarded in scope. **D** `test_artifact_leaks.py` (purge-manifest contract) in a 37-test pass. **E** `audit-og.py` over 95 priority URLs — see row 22. CI's own gate job also green: run 33031794882, `verify-released` ✓. |
| 2 | Lineage: zero prior-vendor figures | 🟡 | Rendered-text sweep of the full built tree (22,927 ZIP pages) + 11 production pages: zero prior-vendor **figures** anywhere; all "Redfin" hits on commercial surfaces are code comments. **Note (owner call, dated 2026-08-27):** `web/research/2026-06/index.html:120` and `2026-07/index.html:120` render "Pre-2026 history is restated from Redfin's archived tracker…" — a provenance *statement*, not a figure, on noindexed research pages; it is however a naming beyond the one permitted methodology reference. Emails: webhook + alert templates reviewed for copy (clean); not every historical template was rendered. |
| 3 | Strings | 🟢 | "verdict": customer copy clean (`verdict-copy.js` renders "reading" throughout); remaining hits are code identifiers (exempt) and internal `admin.html` dashboard labels. `site.webmanifest` said "verdict … from public data" — **fixed in `92dfb95`**. "public housing-market data": absent (one self-documenting comment). "RentCast": rendered only on `/methodology.html`. "home equity": zero occurrences tree-wide. Former-vendor name: row 2's research note only. |
| 4 | Counts consistent, no stale fractions | 🟢 | The single source of truth is now enforced by build: this review began with the crawl gate red — the first on-demand promotion moved the released count to 5,001 and hand-kept `/methodology.html` still said 5,000 (and the parallel deploy had shipped it). **Fixed in `18b4216`**: the pair is stamped at build from the release contract (same derivation the gate checks). Production now renders **5,001 of 22,874** on methodology; homepage, `/zip/`, press are count-free by design; `llms.txt` is generated. Homepage FAQ JSON-LD's stale "5,000" also made count-free in `18b4216`. |
| 5 | LEGAL_HOLD intact | 🟢 | Archive `/Users/yonastodd/SISY-LEGAL-HOLD-2026-08-22/` re-hashed against `PRESERVATION_MANIFEST.md`: **33 recorded / 33 present / 0 missing / 0 unrecorded / 0 mismatches**, 1,087,750,075 bytes, byte-identical to the manifest total. Pages artifact uploads `web/` only; archive is outside the repo; grep of all 5 cron workflows: no destructive step touches held paths. `test_legal_hold.py`: 5 passed. |

## B. Money path

| # | Check | Status | Evidence |
|---|---|---|---|
| 6 | Test purchase, live ZIP | 🟡 | **This stack has no Stripe test mode** — live Payment Links only (docs/STRIPE.md), so no synthetic purchase was made. Production evidence instead: a **real completed report purchase** on 2026-08-26 12:47Z (subscribers: zip 20874, plan `report`, status `active`, source `stripe` — activated by the webhook), report figures served from the v2 reading via `market-reading` (verified: correct word, metrics, 12-month history, dates). Checkout rendering ($5.99/$3.99/$29/$23.01) is pinned by docs/STRIPE.md's regression list. **Note 2026-08-27:** a full synthetic checkout (live card) is an operator action if wanted. |
| 7 | Test purchase, notice ZIP | 🟢 | The row-6 purchase **was** a notice-ZIP on-demand purchase — the feature's first real sale. Sequence verified in prod data: notice lookup → `followed_purchase` 12:46:39 → pre-payment pull 12:47:08 (`subscribe.html:467-503` — "only ok:true lets the Stripe redirect happen; the charge IS the redirect") → validated → charged → delivered. `market_stats` holds `raw_json` (151,684 B), `retrieved_at`, `source='rentcast'`. Page promotion ran (manual sweep, `fe019f3`) and `/zip/20874/` now renders the reading. |
| 8 | Failure path: below-floor ZIP | 🟢 | Live probe: `ondemand-pull {"zip":"10008"}` → `{"ok":false,"reason":"no_data"}`. **No charge is possible**: the Stripe redirect is gated on `ok:true`, and no Stripe session is ever created (no dashboard assertion needed — there is no charge object to assert on). Honest copy verified (`subscribe.html:497`: "…you haven't been charged"). Failure logged as a coverage-gap row (`ondemand_pulls` 10008 `no_data` 02:01:57Z). Notify-me capture works: signup for 10008 → `{"ok":true}`, subscriber row created (waitlist/pending, double-opt-in). |
| 9 | Watch subscription | 🟡 | Zero live watches exist in prod (feature unexercised by customers — vacuously consistent). Mechanism verified in code + suite: `save-watch:132-151` stamps `baselineBasis` from `zip_release.basis` at write time; **the spec's `basis='rentcast_asking'` literal does not exist in this codebase by design** — the released basis literal is `"active listings"`, and introducing `rentcast_asking` would trip `CrossBasisError` (memory + `check_watches` guard). Alert rendering pinned by the pipeline suite; forced-change fixture email **not sent to a live inbox this review** (needs the Resend key — operator step, note dated 2026-08-27). Cancel = Stripe portal (`portal-session`); refunds page accurate (row 21). |
| 10 | Ceiling degradation | 🟢 | Not induced on prod (would mean mutating the live secret). Enforced by suite run fresh today: `test_the_ceiling_is_counted_before_the_vendor_is_called`, `test_an_uncountable_ceiling_refuses_to_spend`, `test_missing_api_key_degrades_to_capacity_not_error` (37/37 pass); at-capacity CTA copy in `subscribe.html:500` ("temporarily at capacity … You haven't been charged"). The identical degradation was observed live by curl on 2026-08-25 while the key was unset. Silent overage impossible: every vendor call is a ledger row counted before spend. |
| 11 | Dedupe | 🟢 | Live: second `ondemand-pull {"zip":"20874"}` → `{"ok":true,"served":"store"}`; the pull ledger still shows **one** vendor row for 20874 (plus 10008's `no_data`) — zero marginal API calls, asserted via the call log (`ondemand_pulls`). Store-before-vendor also pinned by `test_the_store_is_checked_before_the_vendor_is_called`. |

## C. Integrity & resilience

| # | Check | Status | Evidence |
|---|---|---|---|
| 12 | Forms vs hostile input | 🟢 | `demand`: script-tag ZIP → `bad zip`; 10-digit ZIP → `bad zip`; server-reserved outcome (`pull_ok`) → `bad outcome` (all 400-class, fixed JSON, nothing echoed). `signup`: script-tag email → `valid email required`; SQL-injection payload → **blocked by Cloudflare WAF at the edge** before the function; valid capture → row created (pending, double-opt-in; my tokenless curl was counted as `turnstile_bypass` — the configured grace mode, working). ZIP check validates client- and server-side. Contact is `mailto:` (no form). Checkout address form mirrors DB column checks (`save-address`); not exhaustively fuzzed — same validation layer as above. |
| 13 | Error states, 404 | 🟢 | ZIP pages are **fully static** — reading, dials, dates baked in; no runtime call can blank a money section. Homepage: reading word comes from the static record; the endpoint only merges figures inside try/catch ("dials stay empty; the reading still shows"); total failure routes to the honest not-covered branch. No stack traces reachable. 404 was the GitHub Pages default — **fixed in `92dfb95`** (branded `web/404.html`, verified live post-deploy). |
| 14 | admin.html locked | 🟢 | Anonymous RPC probe: `POST /rest/v1/rpc/admin_demand` with the public anon key → **401 `permission denied for function admin_demand`**. Page carries `noindex,nofollow`, is in no sitemap, and no published page links it. Session alone is rejected (`admin_ping` allowlist: "A session is not authorisation"). |
| 15 | Headers, TLS, rate limits | 🟢 | http→https 301 (apex + www); zero mixed content; meta-CSP on every page fetched. `market-reading`: `cache-control: public, max-age=86400, stale-while-revalidate=604800`, CORS locked to the site origin; rate-limited 120/h/IP (35-request burst → all 200, under the cap by design; config read from `_shared/ratelimit.ts`), `ondemand-pull` 10/h/IP + 3/h/IP vendor path. **Risk-register notes (dated 2026-08-27):** GitHub Pages sends no HSTS on custom domains; meta-CSP cannot express `frame-ancestors` (no clickjacking header possible on Pages); `script-src 'unsafe-inline'` is inherent to this architecture. Platform limits, not fixable in-repo. |
| 16 | Performance | 🟢 | PSI API quota-exhausted (Google's shared anonymous pool); local Lighthouse mobile against prod: homepage **0.84, LCP 3.7 s, CLS 0.05**; live ZIP page **1.00, LCP 1.7 s, CLS 0**. Nothing egregious. Note: homepage LCP sits near the 4 s line under mobile throttling — optional optimization. |

## D. Measurement

| # | Check | Status | Evidence |
|---|---|---|---|
| 17 | Demand logging on every lookup path | 🟢 | **Was 🔴, fixed during review (`92dfb95`)**: direct `/zip/` landings wrote nothing to the demand funnel — only homepage checks logged, so the dashboard was blind to organic search traffic landing on notice pages, the exact demand it exists to measure. Fix: build stamps `data-demand-zip`/`data-demand-outcome` on each ZIP page body; `track.js` fires `SISY.demand` on load. Verified live post-deploy (fresh `zip_lookups` row from a production ZIP-page visit). Admin demand report renders: `admin_demand(30)` → totals, top notice ZIPs (01001: 5 lookups, 0 notify — first coverage-gap signal), funnel rates, pull budget 1/ceiling. |
| 18 | Conversion events arrive | 🟡 | Events land server-side (26 h window: 961 `page_view`, 8 `zip_check`, 3 `purchase_click_report`, 1 `turnstile_bypass`). Free check ✓ (`zip_check` + demand outcomes). Notify-me ✓ (`follow(notify)` → `followed_notify` + signup rows). Report purchased ✓ (`purchase_click_report` → webhook → `subscribers` active; observed on the real 20874 sale). **Note 2026-08-27: watch-started and watch-cancelled emit no analytics event** — they are measurable only via `subscribers.watches` and Stripe status changes. Add `watch_started`/`watch_cancelled` events when watch volume starts mattering. |
| 19 | Alerting reaches the inbox | 🟡 | Inventory, honestly: **(a) stale-data** — no email alert exists; staleness surfaces in admin health rows (14-day thresholds) and refresh-workflow annotations only. **(b) gate-failure — CLOSED, live-tested 2026-08-27T02:34:52Z**: GitHub Actions' native failure emails from tonight's two genuinely failed runs (33030565266, 33030617282) **both confirmed received by the operator**. Standing note: `verify-released` and `deploy` are parallel jobs — a gate failure emails you but does **not** stop the deploy. **(c) payment-webhook failure** — Stripe's native failing-endpoint emails to the account owner; in-repo, only the digest path has a Resend failure email. No synthetic sends were made (Resend key is a secret this session cannot read). **Note 2026-08-27:** if inbox-tested alerting is wanted for (a)/(c), it needs building — approximately one Resend call in the refresh job and one in the webhook's error path. |

## E. Content

| # | Check | Status | Evidence |
|---|---|---|---|
| 20 | Crawl for broken/placeholder/dates | 🟢 | Production crawl gate `clean` (row 1): asserts figure/word/vintage consistency, count-free coverage pages, no leaked pair off methodology, redirect stubs judged at landing path. Tree-wide greps: no lorem/placeholder text, no empty `{zip}`-class interpolations found. Coverage caveat: 60-URL sample of the 46,356 templated pages (the gate's standard sampling), all 71 static/other URLs crawled. |
| 21 | Legal pages | 🟢 | `/terms.html` (eff. 2026-08-20 rev.), `/privacy.html` (eff. 2026-08-08 + changelog), `/refunds.html` (eff. 2026-08-06) all load, byte-identical to the tree. Refund language matches the strict no-refund decision verbatim ("Annual plans: payments already made are not refunded or prorated"; reports "not refundable" once delivered; sole exception billing errors). Prices consistent with `prices.js` after the **`92dfb95` fix**: every surface said first-year upgrade **$23** while the Stripe path (the $29 link + $5.99-off coupon) charges **$23.01** — a one-cent advertised undercharge, fixed to $23.01 in prices.js, terms, and the webhook's email mirror (redeployed); the price test now pins exact arithmetic. Informational: terms don't yet describe purchase-time data pulls (nothing contradicts them; §7 covers freshness) — optional sentence for the next terms revision. |
| 22 | OG / social previews | 🟡 | 10-page fresh scrape (home, press, methodology, 3 live ZIPs, 2 notice ZIPs, metro, pricing): titles/descriptions current, no stale claims or banned terms; `og:image`s return 200 with real payloads; live-ZIP cards carry current August data. **Notes 2026-08-27:** `/methodology.html` and `/pricing/` have no OG tags at all (scrapers will guess); `audit-og.py`'s "leak" rule predates the relaunch (80 of 95 priority URLs "leak" = current-basis ratings in OG, now intentional share cards) — the script needs its rule updated to assert basis-correctness instead of absence; one live ZIP (01007) uses the default card where peers get monthly cards — confirm the card-generation tier is as intended. |

## Verdict

**CLEAR TO OPERATE.** No red rows remain: the two reds found during the review (row 4's stale methodology pair, row 17's blind demand funnel) were fixed (`18b4216`, `92dfb95`), deployed, and re-verified against production. Yellow rows 2, 6, 9, 18, 19, 22 ship with the dated notes above. Row 19b (gate-failure emails) closed live-tested 2026-08-27T02:34:52Z — both failure emails confirmed received; the remaining operator items are row 6's optional live-card synthetic purchase, row 2's research-provenance owner call, and recording GitHub ticket #4688700's reply in the ledger.

Also shipped during the review window (same session, before the review was requested): the promotion sweep is now manual-dispatch only and self-triggers its rebuild (`b515db1`, `a063200` — GITHUB_TOKEN pushes never start workflows; observed live and fixed).
